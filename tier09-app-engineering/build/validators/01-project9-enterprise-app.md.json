{
  "1": {
    "type": "ssh",
    "check": "test -d ~/project9",
    "hint": "Create your project folder first: run `mkdir -p ~/project9`.",
    "done": "your project9 folder is created"
  },
  "2": {
    "type": "ssh",
    "check": "test -f ~/project9/.env",
    "hint": "Copy the template inside ~/project9: `cp .env.example .env`.",
    "done": "your .env config file exists"
  },
  "3": {
    "type": "ssh",
    "check": "test \"$(PGPASSWORD=labpass psql -h 127.0.0.1 -U labuser -d labdb -tAc \"select count(*) from information_schema.tables where table_name in ('app_users','sessions','ingest_jobs','chunks')\" 2>/dev/null)\" -ge 4",
    "hint": "Load the schema: `PGPASSWORD=labpass psql -h 127.0.0.1 -U labuser -d labdb -f sql/schema.sql`, then seed with sql/seed.sql.",
    "done": "your database schema and seed data are loaded"
  },
  "4": {
    "type": "ssh",
    "check": "test -d ~/project9/.venv",
    "hint": "Create the virtual environment inside ~/project9: `python3.12 -m venv .venv`.",
    "done": "your virtual environment (.venv) is set up"
  },
  "6": {
    "type": "ssh",
    "check": "curl -sf http://127.0.0.1:8000/health >/dev/null 2>&1 || pgrep -f 'uvicorn app:app' >/dev/null",
    "hint": "Start the API: `nohup uvicorn app:app --host 0.0.0.0 --port 8000 > uvicorn.log 2>&1 &`.",
    "done": "your API is up and answering on port 8000"
  },
  "9": {
    "type": "ssh",
    "check": "test \"$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/admin/users)\" = \"401\"",
    "hint": "The admin route must reject anonymous callers with 401 - make sure the API is running the current app.py.",
    "done": "role-based access control is enforced (anonymous gets 401)"
  },
  "12": {
    "type": "ssh",
    "check": "test \"$(PGPASSWORD=labpass psql -h 127.0.0.1 -U labuser -d labdb -tAc \"select count(*) from ingest_jobs where status='done'\" 2>/dev/null)\" -ge 1",
    "hint": "Enqueue a document via POST /admin/ingest, then drain it: `python worker.py --once`.",
    "done": "the background worker processed an ingestion job"
  },
  "14": {
    "type": "ssh",
    "check": "ls ~/project9/backups/*.dump >/dev/null 2>&1",
    "hint": "Take a backup from ~/project9: `bash backup.sh`.",
    "done": "you have a database backup"
  }
}

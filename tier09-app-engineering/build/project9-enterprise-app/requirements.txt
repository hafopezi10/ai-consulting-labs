# Core - always needed.
psycopg2-binary==2.9.9
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
httpx==0.27.2
pytest==8.3.2

# LLM - only used when ANTHROPIC_API_KEY is set. Safe to install regardless;
# the code falls back to the offline MOCK generator when no key is present.
anthropic==0.68.0

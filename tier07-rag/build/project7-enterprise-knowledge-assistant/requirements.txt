# Core - always needed
psycopg2-binary==2.9.9
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
httpx==0.27.2
pytest==8.3.2

# LLM - only used when ANTHROPIC_API_KEY is set. Safe to install regardless.
anthropic==0.68.0

# Optional real embedding model. LARGE download + needs torch; the code falls
# back to hashing embeddings if this is not installed, so it is commented out
# for the offline CPU box. Uncomment on a box that can afford the download.
# sentence-transformers==3.0.1
